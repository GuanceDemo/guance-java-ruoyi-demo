apiVersion: v1
kind: PersistentVolume
metadata:
  name: mysql-pv-volume
  labels:
    type: local
  annotations:
    "helm.sh/hook-weight": "-5"
spec:
  storageClassName: {{ .Values.storageClass }}
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/var/mysql"